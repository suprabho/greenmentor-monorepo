import PipelineBoard from "../PipelineBoard";

/** Standalone demo board — in-memory cascade over the sample engagement (no auth/DB). */
export default function DemoPage() {
  return <PipelineBoard />;
}
