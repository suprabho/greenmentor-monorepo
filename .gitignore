**/.claude/worktrees

# Dependencies
**/node_modules/

# Next.js / TS build output
**/.next/
**/out/
**/next-env.d.ts
**/*.tsbuildinfo

# Vercel CLI link state (local only — never commit; see .vercel/README.txt)
**/.vercel

# Env / secrets
**/.env
**/.env.*
!**/.env.example
!**/.env.shared.example

# MSCI materiality-map extraction artifacts — authenticated session state + raw
# export (see platform/scripts/extract-msci-materiality.ts). Local only, never commit.
**/scripts/.out/

# Nested product repos — own git remotes (Greenmentor org), managed independently
/greenmentor-in-um/
/greenmentor-in-fe/
/greenmentor-in-be/

# Supabase CLI scratch state
green-mentor-pro/*/supabase/.temp/
