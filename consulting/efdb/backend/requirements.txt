# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Database
sqlalchemy[asyncio]==2.0.45
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.14.0
pgvector==0.3.6

# Auth
python-jose[cryptography]==3.3.0
bcrypt==5.0.0  # used directly in routers/auth.py (passlib dropped — incompatible with bcrypt 5.x)

# AI
anthropic==0.40.0

# Document parsing
openpyxl==3.1.5
pandas==2.3.3
# Unified normalization layer (see app/services/extraction/document_parser.py):
#   liteparse  → PDFs + images: local Tesseract OCR, spatial markdown, bounding boxes
#   markitdown → docx/pptx/odt/rtf/html → markdown (adds new formats)
# liteparse needs system packages tesseract-ocr, libreoffice, imagemagick (see Dockerfile.prod).
liteparse==2.1.1
markitdown[docx,pptx,pdf]==0.1.6

# HTTP (for URL ingestion)
httpx==0.28.1
beautifulsoup4==4.12.3

# Utilities
pycountry==24.6.1  # ISO geo-code mapping for the EnvironDec bulk import (scripts/import_environdec_epds.py)
python-dotenv==1.0.1
pydantic[email]==2.13.4
pydantic-settings==2.7.0
numpy==2.3.2
